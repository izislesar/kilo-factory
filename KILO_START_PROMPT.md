Read AGENTS.md, run bash scripts/bootstrap-beads.sh if Beads is not initialized, run bd prime, then autonomously implement the kilo-factory v1 Beads DAG one ready bead at a time using the factory-builder role; prove Kilo runtime assumptions before depending on them, keep Kilo as the harness with manual visible-TUI model selection, add tests with each change, commit coherent work, close only proven beads, create discovered beads for new scope, never force-reset/push, and continue until no implementation bead is ready or a genuine external/human blocker remains.
