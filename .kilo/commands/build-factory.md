---
description: Start or continue the autonomous implementation of kilo-factory from the Beads DAG.
---

Read `AGENTS.md`, run `bash scripts/bootstrap-beads.sh` if Beads is not initialized, run `bd prime`, then autonomously implement the kilo-factory v1 Beads DAG one ready bead at a time using the current manually selected model; prove Kilo runtime assumptions before depending on them, add focused tests with each change, commit coherent work, close only proven beads, create discovered-from beads for new scope, never force-reset or push, and continue until no implementation bead is ready or a genuine external/human blocker remains.
