---
description: Continue kilo-factory from its current Beads state through production readiness and the final release gate.
---

Read `AGENTS.md` and `docs/PRODUCTION_READINESS.md`; run `bash scripts/bootstrap-beads.sh` unconditionally to materialize the latest production DAG into the existing Beads database; run `bd prime`; then autonomously consume ready non-epic production Beads one at a time using the current manually selected model. Re-anchor on current Git/Beads even if this is a resumed long-lived Kilo session. Preserve the existing deterministic Kilo-native architecture unless production evidence exposes a defect. For real-runtime acceptance Beads, use the installed plugin and actual Kilo/Beads/Git/SQLite path rather than mocks. Create discovered-from Beads for defects/missing production scope, add focused regression coverage, commit coherent work, close only proven Beads, never force-reset or push, continue past isolated blockers while other work is ready, and do not stop merely because unit tests are green or core implementation is complete. Continue until the final production release gate passes or only genuine external/human blockers remain with precise evidence.
